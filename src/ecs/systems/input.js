// src/ecs/systems/queries.js

export const Input = {
    left: new Uint8Array(MAX_ENTITIES),
    right: new Uint8Array(MAX_ENTITIES),
    thrust: new Uint8Array(MAX_ENTITIES),
    brake: new Uint8Array(MAX_ENTITIES),
    fire: new Uint8Array(MAX_ENTITIES),
}